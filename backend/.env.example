DATABASE_URL=postgresql+asyncpg://aqsoft:aqsoft@localhost:5433/aqsoft_health
REDIS_URL=redis://localhost:6380/0
# CRITICAL: Change SECRET_KEY before any deployment. Application will refuse to start with default.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-in-production
# Dev only: set to true to bypass SECRET_KEY enforcement. NEVER use in production.
# ALLOW_DEFAULT_SECRET=true
SNF_ASSIST_URL=http://localhost:8000
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
LLM_PRIMARY=anthropic
CORS_ORIGINS=["http://localhost:5180"]
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
UPLOADS_DIR=./uploads
AUTOCODER_URL=
AUTOCODER_API_KEY=
